{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
  	"bootstrap": "cd packages/common; npm install && cd dist; npm install && cd ../../../packages/frontend; npm install && cd ../../packages/server; npm install",
  	"dev": "cd packages/common; npm run dev & cd ../../packages/frontend; npm run dev & cd ../../packages/server; npm run dev",
  	"build": "cd packages/common && npm run build && cd ../../packages/frontend && npm run build && cd ../../packages/server && npm run build"
  }
}
