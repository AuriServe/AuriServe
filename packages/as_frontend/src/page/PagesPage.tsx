// import * as Preact from 'preact';

import './PagesPage.sass';

// import Title from '../Title';
// import CardHeader from '../CardHeader';

export default function PagesPage() {
	// const [ data ] = useSiteData('pages');

	// return (
	// 	<div class="Page PagesPage">
	// 		<Title>Pages</Title>
	// 		<section class="Page-Card">
	// 			<CardHeader icon="/admin/asset/icon/document-dark.svg" title="Manage Pages"
	// 				subtitle={'Manage site pages and elements.'} />

	// 			{data.pages &&
	// 				<ul class="PagesPage-Pages">
	// 					{Object.keys(data.pages).sort((a, b) => a > b ? 1 : -1).map(p =>
	// 						<li>
	// 							<a className="PagesPage-PageItem" href={'/admin/pages/' + p}>
	// 								<p class="PagesPage-PageItemTitle">{data.pages![p]!.title}</p>
	// 								<p class="PagesPage-PageItemDescription">{data.pages![p].description || <em>No description</em>}</p>
	// 								<p class="PagesPage-PageItemPath">{p}</p>
	// 							</a>
	// 						</li>
	// 					)}
	// 				</ul>
	// 			}

	// 			{!data.pages && <h2 className='PagesPage-Notice'>Loading pages...</h2>}
	// 			{data.pages && Object.keys(data.pages).length === 0 && <h2 className='PagesPage-Notice'>No pages found.</h2>}

	// 		</section>
	// 	</div>
	// );
}
