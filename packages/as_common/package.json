{
  "name": "@aurailus/as_common_build",
  "private": "true",
  "version": "0.0.0",
  "author": "Auri <me@auri.xyz>",
  "homepage": "https://github.com/AuriServe",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon --config nodemon.json",
    "build": "tsc --project tsconfig.json",
    "clean": "find dist -name '*' -not -name 'package.json' -not -name 'dist' -delete"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "nodemon": "^2.0.6",
    "typescript": "^4.1.2"
  }
}
